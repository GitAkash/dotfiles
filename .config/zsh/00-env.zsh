# Language environment (if needed)
# export LANG=en_US.UTF-8

export XDG_CONFIG_HOME="$HOME/.config"
export EDITOR=hx

# Add extra PATHs if needed
# export PATH="$HOME/bin:$HOME/.local/bin:/usr/local/bin:$PATH"
export PATH="$HOME/miniconda3/bin:$PATH" #Add Miniconda to the beginning of the PATH
export PATH="$PATH:/home/akash/.local/bin" #Created by 'pipx'
